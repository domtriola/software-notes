/**
 * Draws a scene
 *
 * @param {!WebGLRenderingContext} gl The WebGL Context
 * @param {!ObjType} programInfo Information about the shader program
 * @param {!ObjType} buffers Buffers
 */
export function drawScene(gl, programInfo, buffers) {
  // drawRectangle(gl, programInfo, buffers);
}
